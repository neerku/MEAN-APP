//Models are the structual custome datatypes, which can be used throughout the application.
// Interface like a class defines how class look like this but cant be instantiated.
export interface Post {
    title: string,
    content: string
}